<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title><%= car.model %></title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1><%= car.make%>  <%= car.model %></h1>
    <p>Engine Type: <%= car.enigine %></p>
    <p>Transmission Type: <%= car.transmission %></p>

    <% if (car.carPics) { %>
        <a href="<%= car.carPics.startsWith('http') ? car.carPics : `https://${car.carPics}` %>">Post car pics</a>
        <% } %>

    <a href="/users/<%= user._id %>/cars/<%= car._id %>/edit">
        Modify this car
      </a>

    <form
    action="/users/<%= user._id %>/cars/<%= car._id %>?_method=DELETE"
    method="POST"
  >
    <button type="submit">Send to junkyard</button>
  </form>

  </body>
</html>